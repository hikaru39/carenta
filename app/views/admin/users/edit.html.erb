<% @page_title = "会員情報の編集" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "会員詳細に戻る", [:admin, @user] %></div>

<%= form_for [:admin, @user] do |form| %>
  <%= render "shared/user_form", form: form %>
  <div><%= form.submit %></div>
<% end %>