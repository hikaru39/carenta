<% @page_title = "商品への画像追加" %>
<h1><%= @page_title %></h1>
<h2><%= @item.name %></h2>

<% url = item_images_path(@item, @image) %>
<%= form_for @image, as: "image", url: url do |form| %>
  <%= render "form", form: form %>
  <div><%= form.submit %></div>
<% end %>
